import {Text} from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

const Contact = () => {  
return (
    <SafeAreaView>
      <Text>This the contact us page</Text>
    </SafeAreaView>
  );
};

export default Contact;
