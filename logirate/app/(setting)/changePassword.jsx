import {Text} from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

const ChangePassword = () => {  
return (
    <SafeAreaView>
      <Text>This is the Change Password Page</Text>
    </SafeAreaView>
  );
};

export default ChangePassword;
